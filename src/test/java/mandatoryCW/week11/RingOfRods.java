package mandatoryCW.week11;

public class RingOfRods {
	/* https://leetcode.com/problems/rings-and-rods/
	 * 
	 * Pseudo Code:
	 * Create 3 set for each color
	 * Iterate the loop to traverse till string length
	 * check if charAt(i) == 'R'
	 * r.add(str.charAt(i+1)-'0');
	 * else if check if charAt(i) == 'G'
	 * g.add(str.charAt(i+1)-'0');
	 * else
	 * g.add(str.charAt(i+1)-'0');
	 * 
	 * 
	 */
	public int ringOfRods() {
		
	}
}
